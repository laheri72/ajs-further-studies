{
  "extends": "stylelint-config-standard",
  "rules": {
    "at-rule-no-unknown": [true, {
      "ignoreAtRules": ["keyframes", "supports", "media"]
    }],
    "block-no-empty": true,
    "no-descending-specificity": null
  }
}
